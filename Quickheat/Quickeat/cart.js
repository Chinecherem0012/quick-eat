const cart_items = JSON.parse(localStorage.getItem("cart-itmes"));
const cartItems_el = document.querySelector(".carrt-item");

cart_items.forEach((item) => {
    const divEl = document.createElement("div");
    divEl.classList.add(" cart-item");

    const innerHtml =''
})

